// Board column definitions (the Kanban column editor). The workspace
// collaboration-network payloads that used to live here went with the Ağ
// screen (2026-09-05); the Explorer map reads GET /api/views/graph instead.
